export function MetricCard({label,value,status='neutral',delta}:{label:string;value:string;status?:string;delta?:string}){return <div className="card metric"><div className="metric-label">{label}</div><div className="metric-value">{value}</div><div className="metric-foot"><span className={`status ${status}`}>{status==='good'?'Good':status==='warn'?'Watch':status==='bad'?'Action needed':'Live'}</span>{delta&&<span>{delta}</span>}</div></div>}
export function Section({title,children,action}:{title:string;children:any;action?:string}){return <section className="card section"><div className="section-head"><h2>{title}</h2>{action&&<button className="ghost">{action}</button>}</div>{children}</section>}
