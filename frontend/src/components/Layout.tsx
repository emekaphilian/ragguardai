import type {ReactNode} from 'react'
const items=[['overview','Overview'],['query','Query Lab'],['runs','Live Runs'],['failures','Failures'],['documents','Documents']]
export default function Layout({page,setPage,children}:{page:string;setPage:(p:string)=>void;children:ReactNode}){
 return <div className="app"><aside className="sidebar"><div className="brand"><span className="brand-dot">RG</span><div><strong>RAGGuard</strong><small>Reliability Control Center</small></div></div><nav>{items.map(([id,label])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}><span className="nav-icon">{id==='overview'?'◈':id==='query'?'⌕':id==='runs'?'◉':id==='failures'?'!':id==='repairs'?'↻':id==='evaluations'?'▦':id==='documents'?'▤':id==='indexes'?'▥':'⚙'}</span>{label}</button>)}</nav><div className="side-foot"><span className="pulse"></span> All core services online</div></aside><main className="main"><header className="topbar"><div><span className="crumb">RAGGUARD / {page.toUpperCase()}</span><h1>AI Reliability Control Center</h1></div><div className="top-actions"><span className="env">DEV ▾</span><span className="healthy"><i/> Healthy</span></div></header>{children}</main></div>
}
