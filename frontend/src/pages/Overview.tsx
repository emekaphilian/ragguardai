import {useEffect,useState} from 'react'; import NarratorPanel from '../components/NarratorPanel'; import {MetricCard,Section} from '../components/Cards'; import {getDashboard} from '../api/client'
const value=(n:any)=>typeof n==='number'?`${Math.round(n*100)}%`:'—'
export default function Overview(){const [data,setData]=useState<any>(null);const [error,setError]=useState('');useEffect(()=>{getDashboard().then(setData).catch(e=>setError(e.message))},[]);if(error)return <div className="page"><Section title="Connection unavailable"><p className="answer">{error}. Start the API at port 8000 to view live workspace data.</p></Section></div>;if(!data)return <div className="page">Loading live workspace…</div>;const m=data.metrics||{};return <div className="page"><NarratorPanel page="Overview" data={data}/><div className="metrics"><MetricCard label="Queries run" value={String(m.total_runs??0)} status="good"/><MetricCard label="Latest retrieval score" value={value(m.context_precision)} status="good"/><MetricCard label="Grounded answer" value={value(m.faithfulness)} status="good"/><MetricCard label="Failure rate" value={value(m.failure_rate)} status={m.failure_rate?'warn':'good'}/><MetricCard label="Index chunks" value={String(data.index.chunks)} status="good"/></div><div className="grid two"><Section title="Current index"><table><tbody><tr><td>Documents indexed</td><td>{data.index.documents}</td></tr><tr><td>Searchable chunks</td><td>{data.index.chunks}</td></tr><tr><td>Retriever</td><td>{data.embedding.model}</td></tr><tr><td>Vocabulary terms</td><td>{data.embedding.dimension}</td></tr><tr><td>Latest retrieval latency</td><td>{data.embedding.latency_ms==null?'—':`${data.embedding.latency_ms} ms`}</td></tr></tbody></table></Section><Section title="Open failures"><div className="failure-list">{data.recent_failures.length?data.recent_failures.map((f:any)=><div key={f.id}><b>{f.type}</b><span>{f.severity} · {f.query}</span></div>):<p className="answer">No failures have been recorded yet.</p>}</div></Section></div></div>}
